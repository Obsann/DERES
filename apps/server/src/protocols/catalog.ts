import { publishedProtocols, unconsciousAdultProtocol } from '@voicesos/protocols';

/** Published protocols the engine may select. Task 6 content. */
export { publishedProtocols, unconsciousAdultProtocol };
