import { PlaceholderPage } from '@/components/PlaceholderPage';

/** Network / connection failure surface — task.md Phase 11 P1. */
export function ConnectionPage() {
  return (
    <PlaceholderPage
      title="Connection problem"
      purpose="Explain network or voice-session loss and what the user should do next."
      task="Task 26 · Frontend failure handling"
    />
  );
}
