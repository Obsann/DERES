# Frontend Vite env. Copy to .env.local for local overrides.
# Never commit real secrets here.

# Backend origin used by the Vite proxy (and later by the API client).
VITE_API_URL=http://localhost:4000
